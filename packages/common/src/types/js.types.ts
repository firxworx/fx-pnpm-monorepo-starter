/**
 * JS primitive types including `bigint`, `symbol`, `undefined`, and `null`.
 */
export type JsPrimitive = string | number | boolean | bigint | symbol | undefined | null

/**
 * JS built-in types: `Date` and `RegExp`.
 */
export type JsBuiltIn = Date | RegExp
